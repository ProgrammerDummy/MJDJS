/*

2. The retry drain task. retry_queue currently accumulates entries that nothing ever removes. Mini-redis shape: peek the front of the BTreeSet, sleep_until that Instant, raced against a Notify in select!; on fire, pull the job from running_jobs, rebuild it as a QueuedJob, insert into job_queue. Without this, every retrying job is permanently stuck.

3. The heartbeat-death detector. Same shape over worker_heartbeat_timer. On fire: mark the worker Dead, iterate its assigned_jobs, and for each — determine_reclaim_event → WorkerLost or DeadLetter → back to job_queue or into completed_jobs. This is the piece that makes infra_interruptions mean anything; it's currently written but never incremented anywhere.

4. cancel_job. Needs to handle a job in any of three places — job_queue (remove directly), running_jobs/Executing (this is where StopAcceptingWork-style signalling or a cancellation token matters), running_jobs/Retrying (remove from retry_queue too — the keyed-removal capability you chose BTreeSet for in the first place).

5. worker-agent. Register on startup, heartbeat loop, RequestWork poll loop, sleep-executor, ReportResult. Nothing runs end-to-end until this exists.

6. Integration + chaos tests. Full lifecycle through real gRPC; then the roadmap's target — SIGKILL a worker mid-job, verify reclaim, ten consecutive runs, zero job loss.

7. Week 7: SQLite persistence, ListJobs, ListDeadLettered, RequeueFromDLQ.

*/